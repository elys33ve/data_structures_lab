#include <string>
#include "Tree.h"



template<class T>
void Tree<T>::insert(T item) {


// first item
// second item

// balance thrid
bal(find_parent(parent))

}

balance(Node<T> *node) {


	if (height(node) != corr_height() && height(node) > 2) {
			balance
	}



}

find parent(node) {
	if node = nullptr {rtn nullptr}
	
	if node== root {rtn root}

}